# Vorlage — die ECHTE Datei liegt NICHT im Repo, sondern auf nas1 unter
#   /home/jens/.config/mcp-recall/consolidation.env   (chmod 600)
# Sie enthaelt das langlebige Abo-Token fuer die Claude-Code-CLI, erzeugt mit
#   claude setup-token
# run.sh laedt sie beim Start (Variable MCP_RECALL_TOKEN_FILE ueberschreibbar).
export CLAUDE_CODE_OAUTH_TOKEN='hier-das-token-aus-claude-setup-token'
